<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "New Game", should appear as action button if possible -->
    <item
        android:id="@+id/action_newGame"
        android:icon="@drawable/ic_action_newgame"
        android:title="@string/action_newGame"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_gamelist"
        android:title="@string/action_gamelist"
        android:icon="@drawable/ic_action_gamelist"
        app:showAsAction="ifRoom"/>


</menu>